## Formatting and Linting

- After making code changes, run: `bun run format && bun run lint:fix`
- Never run format and lint as separate commands - always use the combined command above
- Fix any linting errors before considering the task complete
