## Formatting and Linting

- Always format code with Prettier after making changes
- Run ESLint to check for issues after code modifications
- Use `bun run lint:fix` to automatically fix linting issues
- Use `bun run format` to format code with Prettier
- Fix any linting errors before considering the task complete
