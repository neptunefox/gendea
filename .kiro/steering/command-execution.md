## Command Execution

- Before running any command, explain it in this format: Need to run `command`. Why: [reason]
- Do not run development commands like `bun run dev` or `bun build` - let the user run these manually
- Avoid running long-running processes or build commands during development
